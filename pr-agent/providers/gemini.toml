# Qodo Merge / PR-Agent provider profile.
# Required secret in reusable workflow: gemini_api_key.
# Exported for Qodo compatibility as GOOGLE_AI_STUDIO.GEMINI_API_KEY and GEMINI_API_KEY.

[config]
model = "gemini-2.5-pro"
fallback_models = ["gemini-2.5-pro"]
